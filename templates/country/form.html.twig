{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">

        {{ form_start(form) }}
        {{ form_end(form) }}

        {#{{ dump(country) }}#}
        {#{{ dump(file) }}#}
    </div>

{% endblock %}